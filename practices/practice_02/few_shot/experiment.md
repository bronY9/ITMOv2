# Few-shot

- Артефакт Практики 1: `@practices/practice_01/prompts.md`
- Что хотим улучшить: сделать записи P1-01 и P1-03 конкретными и проверяемыми: добавить evidence (file:line + цитаты) и ссылки на изменённые файлы/разделы вместо обобщений.

## Примеры

### Хороший результат

Короткая запись с конкретикой:

- Риск: «нет валидации входа, возможен KeyError» — evidence: `app/api.py:36-37` — "return review_service.review(payload[\"diff\"])".
- Проверка: HTTP POST `/api/reviews` без `diff` — ожидаем 500/KeyError.
- Изменение: в `practice_01/prompts.md` добавлена ссылка и цитата строки.

### Плохой результат

Обобщение без ссылок и проверок:

- «Проблемы с валидацией и ошибками LLM, нужно поправить». Без указания, где и как проверить.

## Запрос

«Возьми риски из P1-01 и перепиши их в формате: краткая формулировка + evidence (file:line + цитата из TRAINING_PR.diff) + шаг проверки. Удали или отметь как отклонённые пункты без evidence.»

## Что получили

Сформированный список рисков с точными ссылками и проверками (см. изменения ниже).

## Что изменили в исходном артефакте

- Файл и раздел:
- `@practices/practice_01/prompts.md`, строка P1-01 (колонка «Результат»)
- Изменение: добавлены evidence для каждого риска и убрана расплывчатая формулировка про «мелкий стиль в LLM Protocol».
- Как проверили: открыли `TRAINING_PR.diff` и сопоставили цитаты со строками `app/review_service.py:20-22`, `app/api.py:36-37`.
- Что отклонили: любые формулировки без подтверждения в diff.
