# Integration-проверки

| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| FastAPI → ReviewService | Отсутствует ключ `diff` в payload | Вызвать `POST /api/reviews` без поля `diff` | В текущем виде сервер падает с ошибкой (KeyError) | `app/api.py:35-38` |
| ReviewService → LLM | Исключение внутри `LLM.generate` | Замокать LLM, чтобы `generate` бросал исключение | Исключение не перехватывается, запрос завершается ошибкой | `app/review_service.py:19-22` |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-02, P1-03.
- Что проверили и исправили сами: ограничились сценариями, подтверждающимися строками diff.
