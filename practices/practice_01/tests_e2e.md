# E2E-проверки

| Сценарий пользователя | Предусловия | Действие | Наблюдаемый результат | Evidence |
|---|---|---|---|---|
| Позитивный | Запущен сервис FastAPI; доступен POST /api/reviews | Отправить JSON: `{"diff": "test"}` | HTTP 200; тело содержит `{ "comment": <строка> }` | TRAINING_PR.diff `app/api.py:35-38`, `app/review_service.py:19-22` |
| Негативный | Запущен сервис FastAPI | Отправить JSON без ключа `diff` | Ошибка сервера (в текущем виде KeyError → 500) | TRAINING_PR.diff `app/api.py:36-37` |
| Граничный | Запущен сервис FastAPI | Отправить JSON с пустым `diff`: `{"diff": ""}` | HTTP 200; `comment` присутствует (поведение LLM зависит от реализации, здесь не определяется) | TRAINING_PR.diff `app/review_service.py:20-22` |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-02, P1-03.
- Что проверили и исправили сами: подтвердили шаги ссылками на строки diff; не вводили предположений о коде вне TRAINING_PR.diff.
